.title
    .container
        h2!= cart_title
form(action="" method="post")
    .container
        .cart__info
            //- .cart__head
            //-     .title(uk-toggle="target: #show_cart")
            //-         span!= your_order
            #show_cart.show-cart
            .total__price
                span!=total_price
                |&nbsp;
                span.total-cart
                !=currency
            center
                .clear__cart
                    span!= remove_all
        //end container
    //end cart__info    
    .container
        .cart__form 
            input#name(type="text" name="name" placeholder=user_name)

            input#phone.only_number(type="text" name="phone" placeholder=user_phone)

            select#country_code_selector(name="country_code")
                option(value="AE") United Arab Emirates
            button.submit.js_submit
                != submit_title
        //end cart__form
